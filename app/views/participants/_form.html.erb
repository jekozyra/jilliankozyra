<%= form_for(@participant) do |f| %>
  <% if @participant.errors.any? %>
    <div id="error_explanation">
      <h2><%= pluralize(@participant.errors.count, "error") %> prohibited this participant from being saved:</h2>

      <ul>
      <% @participant.errors.full_messages.each do |msg| %>
        <li><%= msg %></li>
      <% end %>
      </ul>
    </div>
  <% end %>

  <div class="field">
    <%= f.label :name %><br />
    <%= f.text_field :name %>
  </div>
  <div class="field">
    <%= f.label :original_name %><br />
    <%= f.text_field :original_name %>
  </div>
  <div class="field">
    <%= f.label :email %><br />
    <%= f.text_field :email %>
  </div>
  <div class="field">
    <%= f.label :profile_link %><br />
    <%= f.text_field :profile_link %>
  </div>
  <div class="field">
    <%= f.label :party_id %><br />
    <%= f.collection_select :party_id, Party.all, :id, :name, {:include_blank => true} %>
  </div>
  <div class="field">
    <%= f.label :list_position %><br />
    <%= f.text_field :list_position %>
  </div>
  <div class="actions">
    <%= f.submit %>
  </div>
<% end %>
